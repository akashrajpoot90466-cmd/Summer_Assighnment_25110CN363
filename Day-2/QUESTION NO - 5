#include <stdio.h>
int main(){
    int N;
    printf("Enter the numer to calculate the sum of digits:");
    scanf("%d",&N);
    int sum=0;
    while(N!=0){
        sum+=N%10;
        N/=10;
    }
    printf("Sum of digits: %d",sum);
    return 0;
}
