#include <stdio.h>
int main(){
    int Num;
    printf("Enter the number to calculate the product of digits:");
    scanf("%d",&Num);
    int product=1;
    while(Num!=0){
        product*=Num%10;
        Num/=10;
    }
    printf("Product of digits: %d",product);
    return 0;
}
